diff --git a/libs/python/computer-server/computer_server/handlers/cua_driver.py b/libs/python/computer-server/computer_server/handlers/cua_driver.py
index 97df86de6..26588ae7b 100644
--- a/libs/python/computer-server/computer_server/handlers/cua_driver.py
+++ b/libs/python/computer-server/computer_server/handlers/cua_driver.py
@@ -14,7 +14,6 @@ import base64
 import importlib
 import json
 import os
-import uuid
 from io import BytesIO
 from types import ModuleType
 from typing import Any, Dict, List, Optional, Protocol, Tuple
@@ -47,6 +46,8 @@ class DriverClient(Protocol):

     async def get_session_state(self, input: Any) -> Any: ...

+    async def get_session(self, input: Any) -> Any: ...
+
     async def get_desktop_state(self, input: Any) -> DriverResult: ...
 
     async def get_screen_size(self, input: Any) -> DriverResult: ...
@@ -108,11 +109,10 @@ class CuaDriverAutomationHandler(BaseAutomationHandler):
         self._driver = driver
         self._fallback = fallback
         self._mode = selected_mode
-        self._session_id = (
-            session_id
-            or os.environ.get("CUA_DRIVER_SESSION_ID")
-            or (f"computer-server-{os.getpid()}-{uuid.uuid4().hex[:8]}")
-        )
+        # CUA Driver 0.20 owns one private lifecycle session per SDK transport.
+        # Never publish computer-server's product name as a session label: the
+        # official overlay renders public labels beside its animated pointer.
+        self._session_id: Optional[str] = None
         selected_scope = (
             (capture_scope or os.environ.get("CUA_DRIVER_CAPTURE_SCOPE", "desktop")).strip().lower()
         )
@@ -132,6 +132,15 @@ class CuaDriverAutomationHandler(BaseAutomationHandler):
                 "CUA_BACKEND=cua-driver requires cua-computer-server[driver]"
             ) from error

+    def _contract_type(self, name: str) -> Any:
+        """Resolve types omitted from the 0.20 wheel's public re-exports."""
+
+        exported = getattr(self._sdk, name, None)
+        if exported is not None:
+            return exported
+        contract = importlib.import_module("cua_driver._native_contract")
+        return getattr(contract, name)
+
     @property
     def mode(self) -> str:
         return self._mode
@@ -151,11 +160,8 @@ class CuaDriverAutomationHandler(BaseAutomationHandler):
             started = await self._driver.start_session(
                 self._sdk.StartSessionInput(
                     session=self._session_id,
-                    capture_scope={
-                        "auto": self._sdk.CaptureScope.AUTO,
-                        "window": self._sdk.CaptureScope.WINDOW,
-                        "desktop": self._sdk.CaptureScope.DESKTOP,
-                    }[self._capture_scope],
+                    capture_scope=None,
+                    cursor_theme=None,
                 )
             )
             if not started.active:
@@ -250,6 +256,32 @@ class CuaDriverAutomationHandler(BaseAutomationHandler):
                 await self._driver.shutdown()
                 self._closed = True
 
+    async def get_agent_cursor_state(self) -> Dict[str, Any]:
+        """Return label-safe state for the transport-owned 0.20 cursor."""
+
+        try:
+            await self._ensure_session()
+            state = await self._driver.get_session(
+                self._contract_type("GetSessionInput")(session=self._session_id)
+            )
+            return self._ok(
+                {
+                    "session": state.session,
+                    "implicit": state.implicit,
+                    "enabled": state.cursor_visible,
+                    "label_visible": state.session is not None,
+                    "theme": {"id": "cua.default"},
+                    "runtime_mode": self._mode,
+                }
+            )
+        except Exception as error:
+            return self._error(error)
+
+    def _desktop_target(self) -> Any:
+        """Select 0.20's exact per-action desktop coordinate space."""
+
+        return self._contract_type("ActionTarget").DESKTOP(display_id="primary")
+
     async def get_capture_scope_state(self) -> Dict[str, Any]:
         """Return the generated session policy state in the legacy envelope."""
 
@@ -320,7 +352,8 @@ class CuaDriverAutomationHandler(BaseAutomationHandler):
                 self._sdk.ClickInput(
                     x=float(px),
                     y=float(py),
-                    scope=self._sdk.DesktopScope.DESKTOP,
+                    target=self._desktop_target(),
+                    scope=None,
                     session=self._session_id,
                     button={
                         "left": self._sdk.ClickButton.LEFT,
@@ -357,7 +390,8 @@ class CuaDriverAutomationHandler(BaseAutomationHandler):
                 self._sdk.MoveCursorInput(
                     x=float(x),
                     y=float(y),
-                    scope=self._sdk.DesktopScope.DESKTOP,
+                    target=self._desktop_target(),
+                    scope=None,
                     session=self._session_id,
                 )
             )
@@ -394,7 +428,8 @@ class CuaDriverAutomationHandler(BaseAutomationHandler):
                 from_y=float(start_y),
                 to_x=float(end_x),
                 to_y=float(end_y),
-                scope=self._sdk.DesktopScope.DESKTOP,
+                target=self._desktop_target(),
+                scope=None,
                 session=self._session_id,
                 duration_ms=min(10000, max(0, int(duration * 1000))),
                 steps=steps,
@@ -440,7 +475,8 @@ class CuaDriverAutomationHandler(BaseAutomationHandler):
             result = await self._driver.type_text(
                 self._sdk.TypeTextInput(
                     text=text,
-                    scope=self._sdk.DesktopScope.DESKTOP,
+                    target=self._desktop_target(),
+                    scope=None,
                     session=self._session_id,
                 )
             )
@@ -454,7 +490,8 @@ class CuaDriverAutomationHandler(BaseAutomationHandler):
             result = await self._driver.press_key(
                 self._sdk.PressKeyInput(
                     key=key,
-                    scope=self._sdk.DesktopScope.DESKTOP,
+                    target=self._desktop_target(),
+                    scope=None,
                     session=self._session_id,
                     modifiers=None,
                 )
@@ -473,7 +510,8 @@ class CuaDriverAutomationHandler(BaseAutomationHandler):
             result = await self._driver.hotkey(
                 self._sdk.HotkeyInput(
                     keys=keys,
-                    scope=self._sdk.DesktopScope.DESKTOP,
+                    target=self._desktop_target(),
+                    scope=None,
                     session=self._session_id,
                 )
             )
@@ -495,7 +533,8 @@ class CuaDriverAutomationHandler(BaseAutomationHandler):
                         "left": self._sdk.ScrollDirection.LEFT,
                         "right": self._sdk.ScrollDirection.RIGHT,
                     }[direction],
-                    scope=self._sdk.DesktopScope.DESKTOP,
+                    target=self._desktop_target(),
+                    scope=None,
                     session=self._session_id,
                     by=self._sdk.ScrollBy.LINE,
                     amount=min(50, max(1, amount)),
diff --git a/libs/python/computer-server/computer_server/main.py b/libs/python/computer-server/computer_server/main.py
index c068f5309..56044f192 100644
--- a/libs/python/computer-server/computer_server/main.py
+++ b/libs/python/computer-server/computer_server/main.py
@@ -416,6 +416,8 @@ if hasattr(automation_handler, "get_capture_scope_state"):
     handlers["get_capture_scope_state"] = automation_handler.get_capture_scope_state
 if hasattr(automation_handler, "escalate_capture_scope"):
     handlers["escalate_capture_scope"] = automation_handler.escalate_capture_scope
+if hasattr(automation_handler, "get_agent_cursor_state"):
+    handlers["get_agent_cursor_state"] = automation_handler.get_agent_cursor_state
 
 
 class AuthenticationManager:
diff --git a/libs/python/computer-server/pyproject.toml b/libs/python/computer-server/pyproject.toml
index 4bdfb86d8..559589ffb 100644
--- a/libs/python/computer-server/pyproject.toml
+++ b/libs/python/computer-server/pyproject.toml
@@ -55,7 +55,7 @@ vnc = [
     "vncdotool>=1.2.0"
 ]
 driver = [
-    "cua-driver>=0.12.2,<0.13.0"
+    "cua-driver>=0.20.0,<0.21.0"
 ]

 [project.urls]
@@ -91,5 +91,5 @@ dev = [
 api = "python -m computer_server"

 [tool.uv.sources]
-cua-core = { workspace = true }
-cua-auto = { path = "../cua-auto", editable = true }
+cua-core = { path = "../core" }
+cua-auto = { path = "../cua-auto" }
diff --git a/libs/python/computer-server/tests/test_cua_driver_handler.py b/libs/python/computer-server/tests/test_cua_driver_handler.py
index e16b2942a..e045fea50 100644
--- a/libs/python/computer-server/tests/test_cua_driver_handler.py
+++ b/libs/python/computer-server/tests/test_cua_driver_handler.py
@@ -12,6 +12,11 @@ class _Record:
         self.__dict__.update(values)


+class _ActionTarget:
+    class DESKTOP(_Record):
+        pass
+
+
 class _CaptureScope(Enum):
     AUTO = "auto"
     WINDOW = "window"
@@ -87,6 +92,14 @@ class _Driver:
             escalation_detail=None,
         )

+    async def get_session(self, input):
+        self.calls.append(("get_session", input))
+        return SimpleNamespace(
+            session=None,
+            implicit=True,
+            cursor_visible=True,
+        )
+
     async def escalate_session(self, input):
         self.calls.append(("escalate_session", input))
         return SimpleNamespace(
@@ -171,10 +184,12 @@ def sdk():
         ClickButton=_ClickButton,
         ScrollDirection=_ScrollDirection,
         ScrollBy=_ScrollBy,
+        ActionTarget=_ActionTarget,
         StartSessionInput=_Record,
         EndSessionInput=_Record,
         EscalateSessionInput=_Record,
         GetSessionStateInput=_Record,
+        GetSessionInput=_Record,
         GetDesktopStateInput=_Record,
         GetScreenSizeInput=_Record,
         GetCursorPositionInput=_Record,
@@ -202,7 +217,7 @@ def fallback():


 @pytest.mark.asyncio
-async def test_desktop_actions_share_one_typed_session(sdk, fallback):
+async def test_desktop_actions_share_one_implicit_session_and_exact_target(sdk, fallback):
     driver = _Driver()
     handler = CuaDriverAutomationHandler(
         fallback,
@@ -225,19 +240,24 @@ async def test_desktop_actions_share_one_typed_session(sdk, fallback):
     assert scroll["success"] is True
     assert [name for name, _ in driver.calls].count("start_session") == 1
     start = next(value for name, value in driver.calls if name == "start_session")
-    assert start.session == "server-a"
-    assert start.capture_scope is _CaptureScope.DESKTOP
+    assert start.session is None
+    assert start.capture_scope is None
+    assert start.cursor_theme is None
     clicked = next(value for name, value in driver.calls if name == "click")
     assert (clicked.x, clicked.y) == (12.0, 34.0)
-    assert clicked.scope is _DesktopScope.DESKTOP
+    assert clicked.target.display_id == "primary"
+    assert clicked.scope is None
+    assert clicked.session is None


 @pytest.mark.asyncio
 @pytest.mark.parametrize(
-    ("capture_scope", "expected"),
-    [("auto", _CaptureScope.AUTO), ("window", _CaptureScope.WINDOW)],
+    "capture_scope",
+    ["auto", "window", "desktop"],
 )
-async def test_session_capture_scope_is_configurable(sdk, fallback, capture_scope, expected):
+async def test_legacy_capture_scope_does_not_label_or_bind_implicit_session(
+    sdk, fallback, capture_scope
+):
     driver = _Driver()
     handler = CuaDriverAutomationHandler(
         fallback,
@@ -250,7 +270,8 @@ async def test_session_capture_scope_is_configurable(sdk, fallback, capture_scop
     await handler.get_screen_size()

     start = next(value for name, value in driver.calls if name == "start_session")
-    assert start.capture_scope is expected
+    assert start.session is None
+    assert start.capture_scope is None


 @pytest.mark.asyncio
@@ -266,13 +287,26 @@ async def test_adapter_uses_the_generated_python_contract(fallback):
     )

     result = await handler.get_screen_size()
+    click = await handler.left_click(12, 34)
+    cursor = await handler.get_agent_cursor_state()

     assert result == {"success": True, "size": {"width": 1280, "height": 720}}
+    assert click["success"] is True
+    assert cursor["success"] is True
     start = next(value for name, value in driver.calls if name == "start_session")
     assert isinstance(start, cua_driver.StartSessionInput)
-    assert start.capture_scope is cua_driver.CaptureScope.DESKTOP
+    assert start.session is None
+    assert start.capture_scope is None
+    assert start.cursor_theme is None
     request = next(value for name, value in driver.calls if name == "get_screen_size")
     assert isinstance(request, cua_driver.GetScreenSizeInput)
+    from cua_driver import _native_contract
+
+    clicked = next(value for name, value in driver.calls if name == "click")
+    assert isinstance(clicked, cua_driver.ClickInput)
+    assert isinstance(clicked.target, _native_contract.ActionTarget.DESKTOP)
+    session = next(value for name, value in driver.calls if name == "get_session")
+    assert isinstance(session, _native_contract.GetSessionInput)


 @pytest.mark.asyncio
@@ -323,10 +357,30 @@ async def test_close_ends_owned_session_and_shuts_down_once(sdk, fallback):
     await handler.close()
     await handler.close()

-    assert [value.session for value in driver.ended] == ["owned-session"]
+    assert [value.session for value in driver.ended] == [None]
     assert driver.shutdown_count == 1


+@pytest.mark.asyncio
+async def test_cursor_diagnostics_keep_the_implicit_session_unlabeled(sdk, fallback):
+    driver = _Driver()
+    handler = CuaDriverAutomationHandler(
+        fallback, driver=driver, sdk=sdk, session_id="must-not-render"
+    )
+
+    state = await handler.get_agent_cursor_state()
+
+    assert state == {
+        "success": True,
+        "session": None,
+        "implicit": True,
+        "enabled": True,
+        "label_visible": False,
+        "theme": {"id": "cua.default"},
+        "runtime_mode": "embedded",
+    }
+
+
 @pytest.mark.asyncio
 async def test_non_portable_actions_remain_on_legacy_handler(sdk, fallback):
     handler = CuaDriverAutomationHandler(fallback, driver=_Driver(), sdk=sdk)
