export const DEMO_RECORDING_INSTRUCTIONS =
  "Demo mode is enabled for this thread. A completed browser demo recording is a required deliverable for this turn. After completing and validating the requested work, use the CUA-backed computer tool inside the selected sandbox to open the browser preview in Google Chrome and record a concise final demo video with the retained recording actions. Inspect the live screen before interacting, wait for the app to be ready, and rehearse the shortest useful demonstration path before starting the recording. Give start_recording and stop_recording the same concise descriptive title. Do not finish the turn until stop_recording returns the completed recording metadata. If a recording action fails, inspect the computer status and retry once when safe. Stop recording promptly. The chat UI embeds the recording automatically from tool output; do not print raw recording URLs, IDs, file paths, or metadata unless the user explicitly asks for them. Skip recording only when no meaningful browser preview is technically possible after checking the available project and desktop state, and explain the concrete blocker.";

export const COMPUTER_USE_WITHOUT_RECORDING_INSTRUCTIONS =
  "Demo mode is disabled for this thread. You may still use the CUA-backed computer tool inside the selected sandbox to inspect and operate browser previews when testing is useful, but do not start a screen recording. The start_recording action is unavailable until demo mode is enabled for this thread.";
